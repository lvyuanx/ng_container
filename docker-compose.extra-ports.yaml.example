version: "3.9"
services:
  nginx:
    ports:
      # 在此添加额外端口映射示例（本文件会提交，作为模板）
      # - "8701:27001"
      # - "8801:28001"
      # - "9000-9002:29000-29002"
      # 如需生效，请复制为 docker-compose.extra-ports.yaml 并按需修改。
